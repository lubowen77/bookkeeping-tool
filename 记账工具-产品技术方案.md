# 赊账记账本 · 产品与技术方案

> 本文档是自包含的开发规格说明，供 AI 编程 agent 或开发者直接实施。
> 配套交互原型：`记账本原型.html`（本目录），UI 布局、用词、交互流程以原型为准。

---

## 1. 背景与目标用户

一位不太懂技术的中年个体经营者，目前用两本纸质账本记赊账：

- **日账本**：每天按笔记录「姓名 + 业务 + 金额」；
- **人头账本**：按客户分页，归集每人所有账目，作为应收欠款台账。
- 每晚人工把日账誊抄归纳进人头账本（本系统要消灭的工序）。

关键事实：

| 项 | 值 |
|---|---|
| 业务性质 | 赊账应收（客户欠款，日后结账） |
| 结账节奏 | 主要集中在年底，少量随时零散结账 |
| 单日笔数 | ≤ 50 笔 |
| 客户总数 | 300–500 人，几乎人人有欠款余额 |
| 使用者 | 1 人、1 台安卓手机；会打字、会发语音、可安装 App |
| 白天场景 | 手脏/手忙，不便操作手机；白天继续手写纸本，**晚间照纸本补录** |
| 网络 | 必须完全离线可用 |

设计总原则：**使用者零技术门槛，维护者（开发发起人）零服务器负担。** 纯本地存储，无账号、无云端、无后端。

## 2. 范围

### v1 必须实现

1. 客户档案（含重名区分、拼音检索）
2. 记一笔（赊账流水，支持补录任意日期）
3. 今天视图（当日流水 + 合计，可改可删）
4. 客户列表（余额实时显示、搜索、按欠款/拼音排序、筛选有欠款）
5. 客户详情 = 结账界面（大字余额、逐笔流水、收款/部分还款/抹零/结清）
6. 期初建档模式（批量：建客户 + 录当前欠款，连续操作）
7. 备份与恢复（每日自动本地备份、一键微信外发、备份文件点开即导入）
8. 回收站（删除即作废，可恢复）
9. 设置：业务快捷按钮配置、字体大小三档

### 明确不做（任何版本未经确认不得加入）

- ❌ 云端同步、账号体系、任何后端服务
- ❌ 手写账本 OCR 识别
- ❌ 多用户、多设备实时同步
- ❌ 打印

### v1.5（2026-08-08 定稿，v1 验收通过后开发）

1. **业务字典**（§3.7）：记账页常用业务大按钮按使用频次自动排序，「更多」打开完整面板——可搜索、可新建、同名检测；
2. **流水编辑与撤销**（§3.5）：确保所有流水类型可编辑；记账/收款保存后 Toast 附「撤销」按钮；
3. **客户账单图片**（§3.8）：一键把某客户的欠款明细生成长图，调系统分享发微信，对方点开即看；
4. **按天拍照留痕**：当天纸本页照片挂在日期下；
5. **备份分轨**（§6 修订）：照片与账目数据永远分开备份；「发送备份给家人」默认只发数据文件（几百 KB）；照片以 zip 单独手动导出，仅在换机迁移时使用；
6. 每周备份外发的本地通知提醒；
7. **金额输入改系统键盘**（§3.9）：移除自绘数字键盘，金额输入统一用手机自带数字键盘；
8. **【缺陷修复】系统组件中文化**：v1 真机实测发现日期选择器显示英文（"Sat, Aug 8" / "August 2026" / 星期缩写 SMTWTFS）。根因是未配置全局本地化，修复要求见 §7 新增条目。

### v1.5.1（2026-08-08 定稿；v1.5 开发完成后追加的措辞专项）

1. **UI 措辞修订**（规则见 §3.1 注、§7「禁用『赊』字」条目）：全 App 去掉「赊」字——`debt` 类型界面显示「记账」（章字「记」）、「记一笔赊账」改「记一笔账」、当日合计「记了 ¥X · 收了 ¥X」、账单图片同步、App 显示名「记账本」。**纯文案替换：数据库中 kind 枚举值 `debt` 及一切存量数据保持不动。**

### v1.5.2（2026-08-08 缺陷修复，已实施）

1. **【Bug】业务字典删除条目后，记账页「更多」宫格残留已删业务名**：根因是「更多」格显示选中业务时未校验其存活状态。修复：渲染时校验存在性、失效自动重置（规则已补入 §3.7「删除/改名的边界规则」）；回归测试 `test/business_dictionary_regression_test.dart`。修改文件：`lib/ui/record/record_page.dart`。

### 后续版本（本期不实现，但架构不得阻碍）

- v2：语音条速记（白天按住说一句存语音，晚间回放着补录）
- v3：语音 AI 转写预填（联网调用识别，预填表单由人确认，错了当场改）

## 3. 业务规则（核心逻辑，必须严格实现）

### 3.1 流水类型与余额公式

流水（entry）共 4 种类型：

| kind | 界面显示名 | 方向 | 说明 |
|---|---|---|---|
| `initial` | 期初旧账 | + | 上线时从纸本迁入的历史欠款，一人通常一条 |
| `debt` | 记账 | + | 日常记的「业务 + 金额」（业务含义=赊账应收） |
| `payment` | 收款 | − | 客户还钱，支持部分还款 |
| `discount` | 抹零 | − | 结账时的减免/优惠 |

> 【v1.5.1 措辞修订】`debt` 的业务含义是赊账应收（本文档行文继续用「赊账」描述逻辑），但**界面一律显示「记账」、类型章单字「记」，全 App（含账单图片的类型列）不得出现「赊」字**——使用者与客户的语言习惯如此，且「赊」字出现在发给客户的账单上观感不佳。

**客户余额 = Σinitial + Σdebt − Σpayment − Σdiscount**（仅计未作废流水）。

余额永远是实时计算或增量维护的派生值，不允许手工直接修改余额；改余额的唯一途径是增改流水。余额可以为负（客户多付/预存），界面显示为「多付 ¥X」。

### 3.2 收款（结账）流程

结账是本系统最重要的场景（年底集中发生）：

1. 找到客户 → 详情页大字显示当前欠款，下方逐笔流水可当面核对；
2. 点「收款」→ 输入实收金额；
3. 实收 < 欠款 → 明确提示「还欠 ¥X」，保存为一条 `payment`（部分还款）；
4. 勾选「剩余 ¥X 抹零，本次结清」→ 同时生成 `payment`（实收）+ `discount`（差额）两条流水，余额归零。抹零金额不设上限，由使用者自行判断；
5. 实收 > 欠款 → 允许，余额变负显示「多付」。

### 3.3 日期与补录

- 每条流水有独立的**业务日期**（biz_date），与录入时间（created_at）分离；
- 记一笔默认日期=今天，一键切「昨天」，也可任选日期（晚间补录、漏记补记的核心场景）；
- 「今天」页可切换查看任意日期的流水。

### 3.4 客户与重名

- 客户字段：姓名（必填，纸本上怎么称呼就怎么记，如「张老三」）、备注（选填，用于重名区分，如「东村」「开货车的」）、拼音首字母（自动生成，冗余存储供检索）；
- 新建客户时若姓名与现有客户完全相同，弹出提示展示已有同名客户（姓名+备注+余额），让使用者确认是「同一个人」还是「再建一个」，杜绝无意识重复建档；
- 搜索支持：汉字、全拼、拼音首字母（如 `zls` → 张老三）。拼音转换用离线库（如 Flutter 的 `lpinyin`），多音字按常见姓氏读音优先。

### 3.5 删除与防误操作

- 一切删除均为**软删除**（打 deleted_at 标记），入「回收站」，可恢复；回收站不自动清空；
- 删除客户前若其余额 ≠ 0，强提示确认；删除客户则其全部流水一并作废；
- 所有删除操作二次确认，确认按钮文案写明后果（「删掉这笔 ¥350 的赊账」）；
- 流水可编辑（金额、业务、日期、客户），编辑直接生效，不做审批流。**此条适用于全部四种流水类型**（收款、抹零、期初同样可改金额/日期），入口统一为「点流水条目 → 改/删」；
- 【v1.5】**保存后撤销**：记一笔、收款保存成功的 Toast 上附「撤销」按钮，停留 5 秒，点击撤回本次产生的全部流水（一次抹零结清撤回收款+抹零两条）并恢复表单内容。这是误触的第一道防线，编辑与回收站是第二道；
- 一次结账生成的「收款 + 抹零」两条流水，事后编辑与删除**互相独立、不做联动**——余额公式对任何单条改动都自洽，不得实现"改一条自动调另一条"的逻辑。

### 3.6 期初建档模式

一次性向导，也可随时从设置进入补录：

- 表单只有三项：姓名 → 备注（选填）→ 当前欠款金额，按「存好，录下一位」连续循环；
- 显示已录人数进度（如「已录 37 人」）；
- 生成一条 `initial` 流水，业务日期 = 录入当天；金额可为 0（只建档不欠款）；
- 允许中途退出、多次进入，直到录完为止。历史明细不迁移，留在旧纸本备查。

### 3.7 业务字典【v1.5】

- 业务从「设置里配置的固定按钮」升级为**业务字典**：一张可增长的业务名录（见 §4 `businesses` 表），类似客户的查找/新增体验；
- **记账页快捷路径不变**：仍显示业务大按钮宫格，取字典中按使用频次（最近 90 天使用次数，次数同则最近使用优先）排序的前 5 个 + 第 6 格「更多…」；
- 「更多…」打开业务面板：搜索框（汉字/拼音）+ 按频次排序的完整列表 + 「＋ 新业务」；新建时同名检测（与客户同名逻辑一致）；选中即回填记账页；
- 新业务自动入字典、随使用频次自然爬升到快捷位。v1 的「业务按钮设置」页在 v1.5 移除，改为「我的 → 业务字典」管理入口（改名、删除，删除入回收站）；
- **快照语义（必须严格实现）**：流水上的 `business` 字段是记账当时业务名的文本快照。日后业务改名/删除，**不回溯**修改任何历史流水——账本当时写的是什么就是什么。`businesses` 表只是输入辅助字典，不与 `entries` 建外键；
- **删除/改名的边界规则（v1.5.2 缺陷修复补充）**：任何"当前选中/记忆中的业务"引用，渲染时必须校验该业务仍存活（未删除），失效则重置为未选中并显示「更多…」——记账页宫格、收款等一切引用处同规则。删除与改名操作完成后必须触发全局数据刷新（revision/通知机制），不得只刷新当前页面。历史流水不受影响（快照语义）。

### 3.8 客户账单图片【v1.5】

- 入口：客户详情页「发账单」按钮；
- 生成一张**长图（PNG）**，调起系统分享面板由使用者选微信联系人发送。图片格式是硬性决定：微信内点开即看、无需任何其他 App、不可篡改；**不使用** PDF/HTML/zip；
- 技术边界（同 §6.3）：安卓无法自动向指定微信联系人发送消息，"推送"的实现上限就是"一键生成 + 分享面板"，选人发送这一步由使用者完成，这是平台限制而非缺陷；
- 账单内容（只含该客户本人数据）：店家称呼（设置里可配，默认不显示）、客户称呼、**「截至 X年X月X日」与生成时间**、当前欠款总额（大字）、逐笔明细（日期/类型/业务/金额，含期初、收款、抹零——收款记录必须展示，这是给对方的对账凭证）、尾注「如有疑问请当面对账」；
- 明细范围默认为**上次结清（余额归零）之后的全部流水**；从未结清过则含期初以来全部。图片按内容自适应高度；
- 渲染实现建议：Flutter 侧用 RepaintBoundary 离屏渲染账单 Widget 导出 PNG（逻辑宽 750px，字号放大保证微信内清晰）。

### 3.9 金额输入【v1.5 修订】

- v1 的自绘数字键盘在 v1.5 **移除**，金额输入（记一笔、收款、期初）统一改用**系统自带数字键盘**（`TextInputType.numberWithOptions(decimal: true)`）；
- 实现注意：键盘弹出时表单必须滚动避让，「记上/收好」按钮不得被键盘遮挡；输入过滤只允许数字与最多两位小数；金额框本身保持大字号显示。

## 4. 数据模型（SQLite）

```sql
CREATE TABLE customers (
  id          INTEGER PRIMARY KEY AUTOINCREMENT,
  name        TEXT NOT NULL,             -- 称呼，纸本口径
  note        TEXT DEFAULT '',           -- 重名区分/备注
  pinyin_full TEXT NOT NULL DEFAULT '',  -- 全拼，小写
  pinyin_abbr TEXT NOT NULL DEFAULT '',  -- 首字母缩写，小写
  created_at  TEXT NOT NULL,             -- ISO8601
  deleted_at  TEXT                       -- NULL=正常
);

CREATE TABLE entries (
  id          INTEGER PRIMARY KEY AUTOINCREMENT,
  customer_id INTEGER NOT NULL REFERENCES customers(id),
  kind        TEXT NOT NULL CHECK (kind IN ('initial','debt','payment','discount')),
  business    TEXT DEFAULT '',           -- 业务名，kind=debt 时有意义
  amount_cents INTEGER NOT NULL CHECK (amount_cents > 0),  -- 金额，分，恒正；方向由 kind 决定
  biz_date    TEXT NOT NULL,             -- 业务日期 YYYY-MM-DD
  note        TEXT DEFAULT '',
  created_at  TEXT NOT NULL,
  updated_at  TEXT NOT NULL,
  deleted_at  TEXT
);
CREATE INDEX idx_entries_customer ON entries(customer_id, deleted_at);
CREATE INDEX idx_entries_date     ON entries(biz_date, deleted_at);

CREATE TABLE settings (            -- key-value：业务按钮JSON、字体档位、备份配置等
  key   TEXT PRIMARY KEY,
  value TEXT NOT NULL
);

CREATE TABLE day_photos (          -- v1.5 使用，v1 建表即可
  id         INTEGER PRIMARY KEY AUTOINCREMENT,
  biz_date   TEXT NOT NULL,
  file_path  TEXT NOT NULL,
  created_at TEXT NOT NULL,
  deleted_at TEXT
);

CREATE TABLE businesses (          -- v1.5 业务字典（§3.7）；不与 entries 建外键，entries.business 保持文本快照
  id          INTEGER PRIMARY KEY AUTOINCREMENT,
  name        TEXT NOT NULL,
  pinyin_full TEXT NOT NULL DEFAULT '',
  pinyin_abbr TEXT NOT NULL DEFAULT '',
  use_count   INTEGER NOT NULL DEFAULT 0,   -- 累计使用次数（简化实现可直接用它排序，"近90天"可后续优化）
  last_used   TEXT,
  created_at  TEXT NOT NULL,
  deleted_at  TEXT
);
-- v1.5 迁移：把 v1 settings 里配置的业务按钮 + 历史流水中出现过的业务名去重后灌入本表，use_count 取历史出现次数

```

要点：

- **金额一律以「分」存整数**，界面输入支持到角/分，显示时整元不带小数、非整元带小数；
- 数据量级（500 客户 × 每年 ≤1.8 万笔）极小，余额直接 SUM 实时计算即可，无需缓存表；客户列表页一条 GROUP BY 聚合查询完成全部余额；
- 时间统一存本地时区 ISO8601 字符串。

## 5. 页面与交互规格

信息架构：底部 4 个 Tab —— **记一笔 / 今天 / 客户 / 我的**。布局、用词、字号层级以原型 `记账本原型.html` 为准，本节只写原型体现不了的行为逻辑。

### 5.1 记一笔（默认首页）

- 三要素从上到下：客户 → 业务 → 金额，全部可视化点选，打字仅在搜索客户和「其他」业务时需要；
- 选客户：全屏面板，顶部搜索框 + 「最近」快捷行（最近 8 个交易过的客户）+ 按拼音分组的完整列表 + 右侧字母索引条；面板内可直接「新建客户」；
- 业务：宫格快捷按钮 +「更多…」。v1 为设置页配置的固定按钮 +「其他」自由输入；v1.5 起改为业务字典驱动（§3.7）：前 5 个高频业务 + 「更多…」面板（可搜索、可新建）；
- 金额：v1 为自绘大数字键盘；v1.5 起改为系统数字键盘（§3.9），金额框大字显示、键盘弹出时按钮避让；
- 保存：大按钮，成功后 Toast 复述全句（「已记上：张老三 送货 ¥350」），表单清空客户与金额、保留日期，连续记账不中断；
- 日期行：默认今天，点击弹「今天/昨天/选日期」。

### 5.2 今天

- 顶部日期导航（← 前一天 | 日期 | 后一天 →，点日期弹日历）；
- 流水列表：时间、客户名、业务、金额；点条目弹出「改 / 删」；
- 底部合计：当日赊账合计、收款合计、笔数；
- 【v1.5】合计下方「当天纸本照片」区：已拍照片横向缩略图 + 「拍一张」按钮。拍照调系统相机，照片挂到**当前查看的日期**名下（补拍昨天的纸本就先切到昨天）；点缩略图全屏查看原件字迹，可删除（软删进回收站）；照片压缩后存 App 私有目录（长边 1600px、质量 80），遵守 §6 分轨规则：不进数据备份文件。

### 5.3 客户

- 搜索框（汉字/全拼/首字母）；
- 筛选：全部 / 有欠款；排序：欠款从多到少（默认）/ 拼音；
- 列表行：姓名 + 备注（小字）+ 右侧余额（欠款红色、结清灰色「已结清」、负数绿色「多付」）；
- 右侧 A–Z 索引条（拼音排序时启用）；
- 底部悬浮「+ 新客户」。

### 5.4 客户详情（结账界面，本系统主战场）

- 顶部特大号余额（全 App 最大字号）+「欠款/已结清/多付」状态词；
- 主按钮「收款」，次按钮「记一笔账」（预选中该客户跳记账页；v1.5.1 措辞修订，此前为「记一笔赊账」）；【v1.5】增加「发账单」按钮（§3.8）；
- 流水按时间倒序，逐笔：日期、类型（赊账/收款/抹零/期初旧账）、业务、金额（赊账红 +、收款绿 −）；供结账时当面逐笔核对；点任意流水条目进入编辑（§3.5：改金额/业务/日期/客户，或删除）；
- 收款弹层：按 3.2 实现；
- 更多菜单：编辑客户资料、删除客户。

### 5.5 我的

- 备份卡片：上次备份时间、自动备份状态、「发送备份给家人」（系统分享面板 → 微信，**默认只含账目数据文件，不含照片**）、「从备份恢复」；【v1.5】照片 zip 导出为独立入口，注明"仅换机时用"；
- 期初建档入口（含进度）；
- 业务快捷按钮设置（增删改排序）；【v1.5】此页移除，改为「业务字典」管理入口（§3.7）；
- 字体大小：标准 / 大 / 特大（全局缩放，见 §7）；
- 回收站；
- 关于（版本号、数据条数统计）。

## 6. 备份与迁移规格（可靠性核心，优先级与记账同级）

### 6.1 备份文件

- 格式：单个 JSON 文件，UTF-8：

```json
{
  "app": "zhangben",
  "schema_version": 1,
  "exported_at": "2026-08-07T21:30:00+08:00",
  "counts": { "customers": 412, "entries": 9873 },
  "checksum": "<data 字段 UTF-8 序列化后的 SHA-256>",
  "data": { "customers": [], "entries": [], "settings": {}, "day_photos": [] }
}
```

- 文件名：`记账备份-20260807-2130.jzb`（自定义扩展名 `.jzb`，本质是 JSON）；
- 不含照片文件本体（照片路径元数据保留；照片迁移 v1.5 单独做 zip 导出）。

### 6.2 自动备份

- 触发：每日首次启动时 + 每次进入后台超过 N 次写操作后（简化实现：每日首启一次即可）；
- 位置：双写 —— App 私有目录（保底）+ 公共 `Download/记账备份/`（经 MediaStore，卸载不丢）；
- 保留最近 60 份，自动清理更旧的；
- 「我的」页永远显示上次备份时间；超过 3 天未成功备份则页内黄条提醒。

### 6.3 外发（离机备份）

- 「发送备份给家人」→ 生成最新备份 → 调起系统分享面板 → 使用者选微信发给家人；
- **外发默认且仅发送账目数据文件（.jzb，几百 KB）**，任何情况下不把照片打进这个文件；照片的离机迁移走独立的「照片打包导出（zip）」入口，仅换机时手动使用（v1.5）；
- 安卓不允许 App 静默代发微信，此步必须人点，这是设计如此而非缺陷；
- v1.5 加每周本地通知提醒外发。

### 6.4 恢复与换机

- App 注册 `.jzb` 文件关联（intent-filter：`ACTION_VIEW` + `ACTION_SEND`，兼容从微信「用其他应用打开」和文件管理器双路径）；
- 新手机流程：装 App → 微信里点开备份文件 → 选「记账本」打开 → 校验 checksum 与 schema_version → 显示摘要（「412 位客户、9873 笔账，备份于 8 月 7 日」）→ 确认导入；
- **导入 = 全量替换**；导入前自动把当前库先备份一份（防手滑覆盖）；
- schema_version 高于当前 App 支持版本时拒绝导入并提示先升级 App。

## 7. 适老化 UI 规范（硬性要求）

- 字号（「标准」档）：正文 ≥18sp，列表主文字 20sp，金额 22sp，客户详情余额 40sp；「大/特大」档全局 ×1.15 / ×1.3；
- 触控目标 ≥48dp，主操作按钮高 ≥56dp；
- 浅色主题为主（高对比、适老），主色墨绿系、欠款红、纸白底（见原型 CSS token）；不做深色模式（刻意决策，降低复杂度与认知负担）；
- 图标必须带文字标签，无纯图标按钮；不依赖滑动手势作为任何功能的唯一入口；
- 用词用账本口语，禁止系统腔：「记一笔/欠款/收款/抹零/结清/期初旧账/回收站」，禁用「新建条目/应收账款/事务/同步」；
- 【v1.5.1】**全 App 禁用「赊」字**：`debt` 类型界面显示「记账」、章字「记」；客户详情次按钮「记一笔账」；当日合计用「记了 ¥X · 收了 ¥X」；账单图片的类型列同样用「记账」。App 名用「记账本」；
- **全局中文本地化（v1 实测缺陷，硬性要求）**：pubspec 引入 `flutter_localizations`，`MaterialApp` 配置 `localizationsDelegates`（Material/Widgets/Cupertino 三件套）、`supportedLocales: [Locale('zh','CN')]`，并**强制 `locale: Locale('zh','CN')`** 不跟随系统语言。验收标准：日期选择器、文本框长按菜单（复制/粘贴）等一切系统组件弹层均为中文；日期显示为「2026年8月8日 周六」式样。另：日期选择器用 `initialEntryMode: DatePickerEntryMode.calendarOnly` 禁用手动键入模式，防止误触进入格式化输入；
- Toast 大字、停留 ≥2.5s；关键操作（收款、删除、导入）的确认文案必须复述对象与金额；
- 所有金额数字 `tabular-nums` 等宽对齐。

## 8. 技术选型

**推荐：Flutter（Dart）+ drift(SQLite)**，打包原生 APK。

理由：

1. 单代码库直出安卓原生包，不依赖厂商 WebView（国产安卓 WebView 版本碎片化，是 Web 容器方案在目标人群手机上的真实风险）；
2. SQLite（drift/sqflite）、文件分享（share_plus）、intent-filter 文件关联、MediaStore 写入均有成熟一等插件；
3. AI 代码生成语料充足，UI 一致性强，适老大字布局可控；
4. 完全离线，无任何运行时云依赖。

备选：Capacitor + Web（浏览器可先行调试是优点；但 WebView 兼容、SQLite 插件、文件关联配置的踩坑成本更高，不推荐首选）。

工程约束：

- 权限最小化：v1 仅存储/通知（MediaStore 写 Download 在 Android 10+ 无需存储权限；相机权限留给 v1.5）；
- `minSdkVersion 26`（Android 8.0，覆盖目标人群旧手机）；
- APK 直装分发（由发起人代装或微信传 APK），不上架商店，避免年审负担；
- 禁止引入任何统计/推送/广告 SDK。

### 8.1 环境准备（编写 agent 动工前必须先完成）

开发机为 macOS。写任何业务代码之前，先把工具链跑通到「空模板 App 能装上真机」：

1. 检查 `flutter doctor`；缺失则安装 Flutter SDK（`brew install --cask flutter` 或官方压缩包）与 Android Studio（含 Android SDK、platform-tools），随后 `flutter doctor --android-licenses` 接受许可；
2. **国内网络必须先设置镜像**，否则 SDK 与依赖下载大概率超时：
   - `export PUB_HOSTED_URL=https://pub.flutter-io.cn`
   - `export FLUTTER_STORAGE_BASE_URL=https://storage.flutter-io.cn`
   - gradle 拉取失败时，在 `android/build.gradle` 仓库配置中改用阿里云镜像（`maven.aliyun.com` 的 google/central 仓库）；
3. `flutter create` 空模板，手机开启 USB 调试，`flutter devices` 能看到设备后 `flutter run` 装上真机——此为环境就绪的验收标准；
4. 发布产物：`flutter build apk --release`，APK 位于 `build/app/outputs/flutter-apk/app-release.apk`，经微信传给目标手机安装。

## 9. 验收清单（交付前逐条自测）

1. [ ] 记一笔全流程 ≤6 次点击（选人→点业务→输金额→保存）；补录昨天的账可行；
2. [ ] 造 500 客户 + 2 万笔流水，客户列表滚动与搜索无卡顿；搜「张老三 / zhanglaosan / zls」均命中；
3. [ ] 部分还款后余额正确；「抹零结清」后余额恰好为 0，流水出现收款+抹零两条；
4. [ ] 实收超过欠款，余额显示「多付」且为绿色；
5. [ ] 删除流水进回收站，恢复后余额还原；删除有欠款客户有强提示；
6. [ ] 杀进程重开数据完整；卸载重装后用 Download 目录备份文件恢复成功；
7. [ ] 备份文件经微信发送到另一台安卓设备，点开可唤起 App 并导入，摘要数字正确；篡改文件内容后导入被 checksum 拦截；
8. [ ] 导入前自动生成了「导入前备份」；
9. [ ] 期初模式连续录 20 人无需中途退出表单；
10. [ ] 飞行模式下以上全部功能可用；
11. [ ] 「特大」字号下所有页面无文字截断、无按钮重叠。

### v1.5 增量验收

12. [ ] 业务字典：记账页快捷按钮为使用频次前 5；「更多」面板搜索命中；新建业务立即可选；给某业务改名后，历史流水仍显示记账当时的旧名；
13. [ ] 编辑：修改一笔收款的金额后客户余额正确；修改日期后流水归入新日期；把一笔账改到另一位客户名下后，两人余额都正确；
14. [ ] 撤销：记一笔后点 Toast「撤销」，流水消失且表单内容恢复；「抹零结清」撤销后收款+抹零两条一起消失、余额还原；
15. [ ] 账单：生成的图片只含该客户数据，含「截至日期」与收款/抹零记录；经微信发到另一台手机，点开即看且字迹清晰；
16. [ ] 金额系统键盘：键盘弹出时「记上/收好」按钮不被遮挡；只能输入数字与最多两位小数；
17. [ ] 备份分轨：「发送备份给家人」的文件为 KB 级且不含任何照片；照片 zip 为独立导出入口；
18. [ ] 中文化：日期选择器全中文（年月、星期、日期均无英文）；文本框长按菜单为中文；App 在系统语言为英文的手机上依然全中文。

### v1.5.1 验收

19. [ ] 措辞：全 App 界面与账单图片中搜索不到「赊」字；流水类型显示「记账」、章字「记」；客户详情次按钮为「记一笔账」；
20. [ ] 无副作用：数据库 kind 值仍为 `debt`，v1.5 已记数据原样完好；记一笔、收款、账单生成主流程回归正常。

## 10. 给编写 agent 的启动提示词（复制即用）

> 请阅读项目根目录的《记账工具-产品技术方案.md》与交互原型《记账本原型.html》，按方案实现 v1 版安卓记账 App。技术栈：Flutter + drift(SQLite)。步骤要求：
> 1. 先按 §8.1 完成环境准备，确认空模板能装上真机，再写业务代码；
> 2. 先输出实现计划（模块拆分与顺序、里程碑），再动工；
> 3. 严格遵守 §3 业务规则、§6 备份与迁移规格、§7 适老化 UI 规范；UI 布局、用词、配色对照原型实现（色彩变量在原型 CSS `:root` 中）；
> 4. 范围以 §2 为准，「明确不做」列表中的任何内容不得实现；需求疑问以本文档为准，不得自行扩展；
> 5. 每完成一个里程碑，按 §9 验收清单中对应条目自测并报告结果，全部完成后逐条过完整清单。

### v1.5 迭代提示词（在已有 v1 代码库上使用）

> 请先阅读项目根目录的《记账工具-产品技术方案.md》（v2.0）与交互原型《记账本原型.html》，然后定位本目录中的 v1 Flutter 工程，在其上开发 v1.5。
>
> 任务范围：实现方案 §2「v1.5」清单的全部 8 条（业务字典、流水编辑与撤销、客户账单图片、按天拍照留痕、备份分轨、每周备份提醒、金额输入改系统键盘、系统组件中文化缺陷修复）。详细规格全文搜索【v1.5】标记，重点章节 §3.5、§3.7、§3.8、§3.9、§4（businesses 表与迁移）、§6、§7。UI 布局、用词、配色对照原型实现（原型即 v1.5 交互，色彩变量在其 CSS `:root`）。
>
> 硬性要求：
> 1. 这是增量开发：不得重写或大规模重构 v1 已有功能，只做清单内的改动；§2「明确不做」列表（云端/账号/OCR/多设备同步）任何情况下不得实现；
> 2. 数据库结构变更必须写 schema 迁移：老版本覆盖升级后，v1 已记的客户、流水、设置一条不丢、余额不变；业务字典按 §4 注释初始化（v1 设置按钮 + 历史流水业务名去重灌入）；
> 3. 中文化修复按 §7 执行（flutter_localizations、强制 `locale: zh_CN`、日期选择器 `calendarOnly`），改完检查所有页面无回归；
> 4. 若发现 v1 实际实现与方案描述不符，以方案为准，并在计划中列出差异；
> 5. 动工前先输出实现计划（模块拆分、顺序、对现有代码的改动面），再开始写代码；
> 6. 每完成一项，按 §9「v1.5 增量验收」第 12–18 条对应条目自测；全部完成后回归 §9 第 1–11 条，逐条报告结果；
> 7. 最后构建 release APK（`flutter build apk --release`），报告产物路径与安装方式。

### v1.5.1 迭代提示词（v1.5 完成后的措辞专项）

> 请阅读项目根目录《记账工具-产品技术方案.md》（v2.1）。v1.5 已完成，现在实现 v1.5.1：仅一项 UI 措辞修订，规格见 §2「v1.5.1」与 §7「禁用『赊』字」条目。具体替换：`debt` 类型界面显示名「赊账」→「记账」、类型章单字「赊」→「记」；按钮「记一笔赊账」→「记一笔账」；当日合计改「记了 ¥X · 收了 ¥X」；账单图片中的同类文案同步；App 显示名「记账本」。**这是纯文案替换：不改数据库结构、不改任何逻辑，数据库中 kind 枚举值 `debt` 与全部存量数据保持不动。**完成后全代码搜索「赊」确认清零（文档注释除外），自测 §9 第 19–20 条，构建 release APK 并报告路径。

## 11. 版本管理约定（git，2026-08-08 起生效）

仓库：工程根目录本地 git，`main` 单分支 + 版本标签，无远端（代码的离机备份可后续加远端，与用户数据备份无关）。方案文档与交互原型随代码同库提交——需求变更先改文档、再改代码、同一提交。

**每个版本迭代必须遵守（写给编写 agent 的硬性流程）：**

1. 动工前确认工作区干净（`git status`）；开发过程可自由提交中间进度；
2. 版本完成（验收通过）时：`pubspec.yaml` 的 `version` 同步为该版本号且 **build number（`+N`）必须递增**（覆盖安装依赖它），如 `1.5.2+4` → `1.6.0+5`；
3. 收尾提交信息格式：`v<版本号>：<一句话概括>`，正文列本版改动要点；提交末尾附 `Co-Authored-By` 署名行；
4. 打附注标签：`git tag -a v<版本号> -m "<概括>"`；
5. 版本号规则：功能版本递增次版本号（v1.6、v1.7…），纯缺陷修复/文案递增修订号（v1.5.1、v1.5.2…）；
6. 回滚基准：任何版本出问题，`git checkout v<上一版本>` 重新构建即可回到可用状态。

当前基线：`v1.5.2`（含 v1 至 v1.5.1 全部功能与业务字典删除残留修复），版本号 `1.5.2+4`。

---

*文档版本：v2.2，2026-08-08：登记 v1.5.2 缺陷修复与 §3.7 删除边界规则、新增 §11 版本管理约定。v2.1，2026-08-08：措辞修订从 v1.5 拆出为 v1.5.1（v1.5 开发已完成）。v2.0，2026-08-08：新增 v1.5 需求（业务字典、流水编辑与撤销、客户账单图片、金额系统键盘、备份分轨、中文本地化缺陷修复）。v1.0：2026-08-07 初版。需求变更时先改本文档，再改代码。*
